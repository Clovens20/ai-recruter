# Copier vers .env.local (non versionne). CRA charge les variables REACT_APP_* au build.

# Vide = proxy CRA vers http://localhost:8000 → baseURL /api
# Renseigne pour appeler un API deploye (ex. Koyeb) depuis le dev local :
# REACT_APP_API_URL=https://ton-service.koyeb.app
REACT_APP_API_URL=https://magnetic-marianne-konektegroupproject-a9cd18b2.koyeb.app